#include <bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:

    p & q == 0 ..

    abs(a - p) + abs(q - b) should be min ..

    first make a < b ..

    if is > then at the end swap p and q..

    now i either take some p itself and q if they dont have any bit in common..

    else..

    i take out some bits from p and some bits from q that are common..

    that is i think that among these common bits i assign some to p and some to q..


    its like take not take..


    recursion is going to have an mask..

    this can be big if i store the number..









*/
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
    }
    return 0;
}
