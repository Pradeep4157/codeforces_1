#include <bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:

    when will it trap me :

        no matter what direction i come from it is going to add an bump on the opp side of the

        direction from which i came, now the only way it is possible is that it is in some corner..

        also there is some even condition without which its still not possible..

        it is still possible..

        basic things that if its at start / end then impossible..

        if without any holes i had to move from start -> end without rep in even its  imp..

        and odd its poss.

        and in this case its opp..

        so if n is odd then also imp..

        now in even also some cannot be done

        whereas some can be skipped..

        if i + j sum is even then we cannot skip..

        else we can..



*/
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
    }
    return 0;
}
